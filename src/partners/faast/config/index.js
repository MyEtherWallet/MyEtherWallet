import { FaastCurrencies } from './currencies';
import {
  supportedNetworks,
  statuses,
  notificationStatuses,
  host,
  TIME_SWAP_VALID,
  PROVIDER_NAME,
  FEE_RATE,
  AFFILIATE_ID
} from './config';

export {
  FaastCurrencies,
  supportedNetworks,
  statuses,
  notificationStatuses,
  host,
  TIME_SWAP_VALID,
  PROVIDER_NAME,
  FEE_RATE,
  AFFILIATE_ID
};
