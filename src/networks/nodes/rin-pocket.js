import { RIN } from '../types';
export default {
  type: RIN,
  service: 'pokt.network',
  url: 'pokt.network/rinkeby', // not a real url (decentralized provider)
  port: 443,
  auth: false,
  username: '',
  password: ''
};
