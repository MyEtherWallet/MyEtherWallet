/**
 * Transaction types
 */
const txTypes = {
  success: 'success',
  error: 'error',
  pending: 'pending'
};
/**
 * Notification types
 */
const notificationTypes = {
  swap: 'swap',
  in: 'in',
  out: 'out',
  error: 'error'
};

export { txTypes, notificationTypes };
