import {
  type,
  notificationType,
  notificationStatuses,
  swapOnlyStatuses,
  swapIndexes,
  txIndexes
} from './config';

import {
  addUpdateNotification,
  addUpdateSwapNotification
} from './notificationFormatter';

export {
  type,
  notificationType,
  notificationStatuses,
  swapOnlyStatuses,
  swapIndexes,
  txIndexes,
  addUpdateNotification,
  addUpdateSwapNotification
};
