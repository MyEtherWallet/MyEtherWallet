import Vue from 'vue';
import store from '@/store';
import GenerateTx from '@/layouts/InterfaceLayout/components/GenerateTx/GenerateTx.vue';

xdescribe('GenerateTx.vue', () => {
  // Before(() => {
  //
  // });
  it('does nothing', () => {

  });
  // it('should render correct contents', () => {
  //   const Constructor = Vue.extend(GenerateTx);
  //   const vm = new Constructor({
  //     propsData: {
  //       gasLimit: 50,
  //       nonce: 0
  //     }
  //   }).$mount();
  //   expect(vm.$el.style['background-image']).toEqual('');
  // });

  describe('GenerateTx.vue Methods', () => {});
});
